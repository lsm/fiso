apiVersion: fiso.io/v1alpha1
kind: FlowDefinition
metadata:
  name: test-invalid-flow
  namespace: fiso-system
spec:
  source:
    type: kafka
    config:
      brokers: kafka:9092
      topic: test-events
  sink:
    type: unsupported-sink
    config:
      url: http://nowhere:8080

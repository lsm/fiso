name: transform-flow
source:
  type: http
  config:
    listenAddr: ":8081"
    path: /ingest
interceptors:
  - type: wasm
    config:
      module: /etc/fiso/modules/transform.wasm
      runtime: wasmer
sink:
  type: http
  config:
    url: http://user-service:8082
    method: POST
